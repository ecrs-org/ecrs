/// This trait describes common behaviur of individual (chromosome) encoding.
///
/// It can be impelmented on user's custom struct, so it can be consumed by
/// our algorithms implementations.
pub trait Individual {
	// pub fn mutate
}